name: psw-sqli-13-visible-error-based

services:
  db:
    image: postgres:16-alpine
    container_name: psw-sqli-13-visible-error-based-db
    hostname: db
    environment:
      POSTGRES_DB: labdb
      POSTGRES_USER: labuser
      POSTGRES_PASSWORD: labpass
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U labuser -d labdb"]
      interval: 2s
      timeout: 3s
      retries: 15
    labels:
      - "lab.suite=portswigger"
      - "lab.topic=sql-injection"
      - "lab.id=psw-sqli-13-visible-error-based"
      - "lab.role=database"
    networks:
      - psw-sqli-13-visible-error-based-net

  web:
    build: .
    image: psw-sqli-13-visible-error-based:latest
    container_name: psw-sqli-13-visible-error-based
    hostname: psw-sqli-13-visible-error-based
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "127.0.0.1:18013:8000"
    restart: "no"
    environment:
      - DJANGO_SETTINGS_MODULE=sqli_lab.settings
      - POSTGRES_DB=labdb
      - POSTGRES_USER=labuser
      - POSTGRES_PASSWORD=labpass
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
    labels:
      - "lab.suite=portswigger"
      - "lab.topic=sql-injection"
      - "lab.id=psw-sqli-13-visible-error-based"
      - "lab.title=SQLi - visible error-based data exfiltration"
    networks:
      - psw-sqli-13-visible-error-based-net

networks:
  psw-sqli-13-visible-error-based-net:
    name: psw-sqli-13-visible-error-based-net
    driver: bridge
