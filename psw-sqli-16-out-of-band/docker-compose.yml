name: psw-sqli-16-out-of-band

services:
  db:
    image: postgres:16-alpine
    container_name: psw-sqli-16-out-of-band-db
    hostname: db
    environment:
      POSTGRES_DB: labdb
      POSTGRES_USER: labuser
      POSTGRES_PASSWORD: labpass
    volumes:
      - ./db-init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U labuser -d labdb"]
      interval: 2s
      timeout: 3s
      retries: 15
    labels:
      - "lab.suite=portswigger"
      - "lab.topic=sql-injection"
      - "lab.id=psw-sqli-16-out-of-band"
      - "lab.role=database"
    networks:
      - psw-sqli-16-out-of-band-net

  oob:
    build: ./oob
    image: psw-sqli-16-out-of-band-oob:latest
    container_name: psw-sqli-16-out-of-band-oob
    hostname: oob
    restart: "no"
    labels:
      - "lab.suite=portswigger"
      - "lab.topic=sql-injection"
      - "lab.id=psw-sqli-16-out-of-band"
      - "lab.role=oob-recorder"
    networks:
      - psw-sqli-16-out-of-band-net

  web:
    build: .
    image: psw-sqli-16-out-of-band:latest
    container_name: psw-sqli-16-out-of-band
    hostname: psw-sqli-16-out-of-band
    depends_on:
      db:
        condition: service_healthy
      oob:
        condition: service_started
    ports:
      - "127.0.0.1:18016:8000"
    restart: "no"
    environment:
      - DJANGO_SETTINGS_MODULE=sqli_lab.settings
      - POSTGRES_DB=labdb
      - POSTGRES_USER=labuser
      - POSTGRES_PASSWORD=labpass
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - OOB_LOG_URL=http://oob:8080/log
      - OOB_HOSTNAME=oob
    labels:
      - "lab.suite=portswigger"
      - "lab.topic=sql-injection"
      - "lab.id=psw-sqli-16-out-of-band"
      - "lab.title=SQLi blind - out-of-band interaction"
    networks:
      - psw-sqli-16-out-of-band-net

networks:
  psw-sqli-16-out-of-band-net:
    name: psw-sqli-16-out-of-band-net
    driver: bridge
